# Dsivio

Dsivio 是一个桌面 AI 助手：屏幕级工具（划词翻译、截图 OCR、Lens 视觉问答）+ 一个完整的 agent 聊天应用。聊天既可以跑 Dsivio 自己的 agent 循环，也可以由用户本机安装的外部编码 CLI 来承担。

当前名称为 Dsivio；历史代码中的 `kivio`、`com.zmair.kivio` 和兼容字段沿用原标识。术语核对日期：2026-09-10。

## Language

### 运行时与会话

**外部 CLI 代理**：
用户本机已安装、被 Dsivio 驱动来充当某条对话后端的编码 CLI（claude、codex、grok、kimi、gemini、opencode、hermes、pi、cursor、dsh、antigravity）。
_Avoid_: 第三方 agent、外部模型

**内置运行时**：
Dsivio 自己的 agent 循环。与外部 CLI 代理是同一条对话上二选一的关系。
_Avoid_: 本地 agent、原生循环

**原生会话**：
外部 CLI 自己维护的那份对话历史，存在该 CLI 自己的目录或数据库里，由该 CLI 自己的会话 id 标识。Dsivio 不持有它的真身。
_Avoid_: CLI 会话、上游会话

**会话绑定**：
一条 Dsivio 对话与一条原生会话之间的对应关系。绑定存在时，这条对话的续聊由对应的 CLI 用原生会话 id 承担。
_Avoid_: 会话映射、session 关联

**工作目录**：
一条原生会话被创建时所在的目录。它同时决定两件事：这条会话属于哪个项目，以及某些 CLI 能不能续上它。
_Avoid_: cwd、工作区、workspace

### 上下文

**清空上下文**：
用户手动切断当前对话发给模型的历史窗口。切断点之前的消息仍显示在时间线上，但不再进入后续请求，也不再进入压缩。仅内置运行时（Dsivio Agent / Dsivio Chat）。
_Avoid_: 清除对话、删除上下文、reset context、new chat

**换模型回答**：
对最后一轮同一条用户问题再请另一个模型作答。已有回答保留，新回答进入多答组；续聊默认用新回答。仅内置运行时（Dsivio Agent / Dsivio Chat），规划模式不可用。
_Avoid_: 切换模型、重新生成、@模型

### 侧栏分组

**集**：
助手之上的人设分组，不带根目录；持有自己的系统提示词和默认助手。一条对话要么属于一个集，要么属于一个项目，二者互斥。
_Avoid_: 分组、文件夹、collection

**项目**：
带根目录的对话分组。该目录决定这个项目下的对话默认在哪读写文件。
_Avoid_: 文件夹、workspace

**附加目录**：
一条对话在项目主目录之外额外授权给 agent 的文件夹。不改变这条对话属于哪个项目，也不改变导入/续聊用的工作目录。相对路径仍落在主目录；读写附加目录须用绝对路径。
_Avoid_: workspace、多根工作区、cwd、第二个项目

**手动顺序**：
集和项目在侧栏里自上而下的排列，只由用户拖拽决定，不随时间或使用频率变化。

**钉住**：
把集/项目里的某条对话固定在某一行。该分组内其余对话仍按最近更新倒序填在剩下的位置。「最近」列表里的对话不可钉。
_Avoid_: 置顶、锁定、手动排序对话

### 导入

**导入**：
把一条已存在的原生会话变成一条 Dsivio 对话：历史被解析出来用于显示，同时建立会话绑定。导入不复制历史的所有权——真身仍在 CLI 那边。
_Avoid_: 迁移、同步、导入历史

**续聊**：
在一条已导入的对话上继续对话。始终由原来那个 CLI 承担，不能改由内置运行时或另一个 CLI 接手。
_Avoid_: 恢复、resume、接管

### 图片与视频

**工作台**：
图片、视频的可视化制作页面。内置聊天通过同一个 `studio` 工具操作任务、模板与共享草稿。

**任务**：
有独立 ID、版本与执行状态的图片或视频制作记录。它不是聊天对话，也不是尚未保存的表单内容。

**共享草稿**：
聊天与页面共用的未提交编辑内容，有独立于任务的版本号。本地恢复副本用于恢复编辑，不能无条件覆盖共享版本。

**试品确认**：
用户认可当前规则下的试做效果。图片工作台允许直接生成，试品确认是可选流程；Agent 质检意见不等于用户确认。

**剧本确认**：
用户确认视频任务当前版本的拍摄内容，是视频提交的前提。修改需求或剧本后需要重新确认。

**待核查**：
视频提交结果不确定且没有可靠回执的状态。先核对供应商记录或补录远程编号，不能自动再次提交或删除任务。

**删除任务**：
永久清除任务记录及可确认归属的专属素材副本、生成文件；其他任务、共享草稿或模板仍引用的文件保留。原始导入文件不删除。
_Avoid_: 归档、隐藏、移到回收站
