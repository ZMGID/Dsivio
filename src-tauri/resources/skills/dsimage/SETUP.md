# Dsivio 内置版配置

本 Skill 随应用安装、发现与更新，不需要额外安装或运行 `update`。

1. 在应用「设置 → 供应商」配置图片接口地址及 API key。
2. 在「图片 → 图片设置」选择相同供应商、图片模型和接口协议。
3. 回到聊天运行原来的 `dsimage.py` 命令即可，脚本直接读取共用配置。

不要在 Skill 目录保存 `.env`，也不要回显供应商设置文件。`studio paths` 只输出数据目录，不包含凭据。OAuth-only 供应商的系统凭据无法由 Python 脚本读取，请为图片 API 配置 API key。
